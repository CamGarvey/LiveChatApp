mutation CreateMessage(
  $channelId: Int!
  $createdById: Int!
  $content: String!
) {
  createMessage(
    channelId: $channelId
    createdById: $createdById
    content: $content
  ) {
    id
    content
    createdAt
  }
}
