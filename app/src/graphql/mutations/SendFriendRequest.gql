mutation SendFriendRequest($friendId: HashId!) {
  sendFriendRequest(friendId: $friendId) {
    recipient {
      id
      ... on Stranger {
        friendRequest {
          ...RequestInfo
        }
      }
    }
  }
}
