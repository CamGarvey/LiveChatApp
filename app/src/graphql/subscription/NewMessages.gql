subscription GetNewMessages($channelId: String!) {
  newMessage(channelId: $channelId) {
    message {
      id
      content
      createdAt
      createdBy {
        id
        name
        username
      }
    }
  }
}
