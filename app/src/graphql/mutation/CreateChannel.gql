mutation CreateChannel(
  $name: String!
  $description: String
  $isPrivate: Boolean
  $memberIds: [String!]
) {
  createChannel(
    name: $name
    description: $description
    isPrivate: $isPrivate
    memberIds: $memberIds
  ) {
    id
  }
}
