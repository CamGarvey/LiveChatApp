mutation CreateChannel(
  $name: String!
  $description: String
  $isPrivate: Boolean
  $memberIds: [Int!]
) {
  createChannel(
    name: $name
    description: $description
    isPrivate: $isPrivate
    memberIds: $memberIds
  ) {
    id
  }
}
