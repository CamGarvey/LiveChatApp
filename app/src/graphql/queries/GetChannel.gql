query GetChannel($channelId: String!) {
  channel(channelId: $channelId) {
    id
    name
    createdAt
    description
    createdBy {
      id
      ...NameParts
    }
    members {
      id
      ...NameParts
      friendStatus
    }
  }
}
