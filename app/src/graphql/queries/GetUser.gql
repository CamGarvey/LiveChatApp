query GetUser($userId: HashId!) {
  user(userId: $userId) {
    __typename
    id
    username
    name
    ... on Stranger {
      friendRequest {
        id
        status
        isCreator
      }
    }
  }
}
